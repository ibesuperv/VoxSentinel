# Contributing to VoxSentinel

First off, thank you for considering contributing to VoxSentinel! It's people like you that make the open source community such an amazing place to learn, inspire, and create.

## 🛠️ Development Workflow

1.  **Fork** the repository.
2.  **Clone** your fork locally.
3.  **Create a Branch** for your feature (`git checkout -b feature/amazing-feature`).
4.  **Commit** your changes (`git commit -m 'Add some amazing feature'`).
5.  **Push** to the branch (`git push origin feature/amazing-feature`).
6.  **Open a Pull Request**.

## 🧪 Testing Guidelines

*   **Backend**: Ensure all new endpoints have coverage in `tests/`. Run `pytest` before committing.
*   **Frontend**: UI components should be validated against the design system in `src/index.css`.

## 💻 Coding Standards

*   **Python**: Follow PEP 8 style guidelines.
*   **TypeScript**: Use strict type checking (Avoid `any`).
*   **Commits**: Use [Conventional Commits](https://www.conventionalcommits.org/) (e.g., `feat: allow gpu inference`).

---

*Verified by Varun B.*
